<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Frontend Developer: Today's Tasks</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome for icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --sidebar-width: 250px;
            --navbar-height: 60px;
            --primary-color: #4e73df;
            --secondary-color: #858796;
        }
        
        body {
            font-family: 'Nunito', sans-serif;
            background-color: #f8f9fc;
            overflow-x: hidden;
        }
        
        #sidebar {
            width: var(--sidebar-width);
            height: 100vh;
            position: fixed;
            left: 0;
            top: 0;
            background: linear-gradient(180deg, var(--primary-color) 10%, #224abe 100%);
            color: white;
            transition: all 0.3s;
            z-index: 1000;
            padding-top: var(--navbar-height);
        }
        
        #sidebar.collapsed {
            margin-left: calc(-1 * var(--sidebar-width));
        }
        
        #main-content {
            margin-left: var(--sidebar-width);
            padding-top: var(--navbar-height);
            transition: all 0.3s;
        }
        
        #main-content.expanded {
            margin-left: 0;
        }
        
        #navbar {
            height: var(--navbar-height);
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1001;
            background-color: white;
            box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.15);
        }
        
        .sidebar-link {
            color: rgba(255, 255, 255, 0.8);
            padding: 1rem;
            display: block;
            text-decoration: none;
            transition: all 0.3s;
        }
        
        .sidebar-link:hover, .sidebar-link.active {
            color: white;
            background-color: rgba(255, 255, 255, 0.1);
        }
        
        .card {
            border: none;
            border-radius: 0.35rem;
            box-shadow: 0 0.15rem 1.75rem 0 rgba(58, 59, 69, 0.1);
        }
        
        .tab-content {
            padding: 1.5rem;
            background-color: white;
            border-radius: 0 0 0.35rem 0.35rem;
        }
        
        @media (max-width: 768px) {
            #sidebar {
                margin-left: calc(-1 * var(--sidebar-width));
            }
            
            #sidebar.collapsed {
                margin-left: 0;
            }
            
            #main-content {
                margin-left: 0;
            }
            
            #main-content.expanded {
                margin-left: var(--sidebar-width);
            }
        }
    </style>
</head>
<body>
    <!-- Navbar -->
    <nav id="navbar" class="navbar navbar-expand-lg navbar-light bg-white shadow-sm">
        <div class="container-fluid">
            <button class="btn btn-link text-dark" id="sidebarToggle">
                <i class="fas fa-bars"></i>
            </button>
             <a class="navbar-brand ms-2 d-flex align-items-center" href="#" style="font-size: 1.5rem; font-weight: bold; color: #4e73df;">
    <img src="/images/logofilmfussion.webp" alt="Logo" style="height: 35px; width: auto; margin-right: 8px;">
    <span><i>FILM<span style="color: rgb(20, 114, 25); font-weight: bold;">Fussion</span></i></span>
</a>

            <div class="d-flex align-items-center">
                <div class="dropdown">
                    <button class="btn btn-link dropdown-toggle" type="button" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="fas fa-user-circle fa-lg"></i>
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="dropdownMenuButton">
                        <li><a class="dropdown-item" href="#"><i class="fas fa-user me-2"></i>Profile</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="#"><i class="fas fa-sign-out-alt me-2"></i>Logout</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </nav>

    <!-- Sidebar -->
    <div id="sidebar">
        <div class="sidebar-header py-3 px-4">
            <h5 class="mb-0">Today's Tasks</h5>
        </div>
        <hr class="sidebar-divider my-0">
        <div class="sidebar-menu">
            <ul class="list-unstyled">
                <li>
                    <a href="#dashboard" class="sidebar-link active" data-tab="dashboard">
                        <i class="fas fa-tachometer-alt"></i> Dashboard Overview
                    </a>
                </li>
                <li>
                    <a href="#movies" class="sidebar-link" data-tab="movies">
                        <i class="fas fa-film"></i> 🎥 Movies
                    </a>
                </li>
                <li>
                    <a href="#watchlist" class="sidebar-link" data-tab="watchlist">
                        <i class="fas fa-clock"></i> 🗓 Watchlist
                    </a>
                </li>
                <li>
                    <a href="#ratings" class="sidebar-link" data-tab="ratings">
                        <i class="fas fa-star"></i> ⭐ Ratings & Reviews
                    </a>
                </li>
                <li>
                    <a href="#userProfile" class="sidebar-link" data-tab="user-profile">
                        <i class="fas fa-user"></i> 👤 User Profile
                    </a>
                </li>
                <li>
                    <a href="#logout" class="sidebar-link">
                        <i class="fas fa-sign-out-alt"></i> 🚪 Logout
                    </a>
                </li>
            </ul>
        </div>
    </div>

   
            <!-- Movies Tab -->
<div id="main-content" class="tab-pane fade show active" id="movies" role="tabpanel">
  <div class="card mb-4">
    <div class="card-header py-3">
      <h6 class="m-0 font-weight-bold text-primary">🎬 Movie Collection</h6>
    </div>
    <div class="card-body">

      <!-- Filters Section -->
      <div class="row mb-4">
        <!-- Genre -->
        <div class="col-md-3 mb-2">
          <label class="form-label">Genre</label>
          <select class="form-select">
            <option>All</option>
            <option>Action</option>
            <option>Comedy</option>
            <option>Drama</option>
            <option>Horror</option>
            <option>Sci-Fi</option>
          </select>
        </div>

        <!-- Language -->
        <div class="col-md-3 mb-2">
          <label class="form-label">Language</label>
          <select class="form-select">
            <option>All</option>
            <option>English</option>
            <option>Hindi</option>
            <option>Spanish</option>
            <option>French</option>
          </select>
        </div>

        <!-- Release Year -->
        <div class="col-md-3 mb-2">
          <label class="form-label">Release Year</label>
          <input type="range" class="form-range" min="1990" max="2025" value="2025">
        </div>

        <!-- Rating Slider -->
        <div class="col-md-3 mb-2">
          <label class="form-label">Rating</label>
          <input type="range" class="form-range" min="1" max="10" value="10">
        </div>

        <!-- Actor -->
        <div class="col-md-4 mb-2">
          <label class="form-label">Actor</label>
          <input type="text" class="form-control" placeholder="Search by actor">
        </div>

        <!-- Actress -->
        <div class="col-md-4 mb-2">
          <label class="form-label">Actress</label>
          <input type="text" class="form-control" placeholder="Search by actress">
        </div>

        <!-- Director -->
        <div class="col-md-4 mb-2">
          <label class="form-label">Director</label>
          <input type="text" class="form-control" placeholder="Search by director">
        </div>
      </div>

      <!-- Movies Grid -->
      <div class="row">
        <!-- Movie Card -->
        <div class="col-md-4 mb-4">
          <div class="card h-100">
            <img src="https://via.placeholder.com/300x400" class="card-img-top" alt="Movie Poster" loading="lazy">
            <div class="card-body">
              <h5 class="card-title">Inception</h5>
              <p class="mb-1"><strong>Genre:</strong> Sci-Fi | <strong>Language:</strong> English</p>
              <p class="mb-1"><strong>Rating:</strong> ⭐⭐⭐⭐⭐ 9.0</p>
              <p class="mb-1"><strong>Price:</strong> ₹199 (Rent)</p>
              <p class="text-muted">A mind-bending thriller that explores dream manipulation and subconscious heists.</p>
              <a href="#" class="btn btn-sm btn-outline-primary mt-2"><i class="fas fa-plus"></i> Add to Watchlist</a>
            </div>
          </div>
        </div>

        <!-- Add more cards here dynamically using EJS if needed -->
      </div>

    </div>
  </div>
</div>

    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- jQuery -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    
    <script>
        $(document).ready(function() {
            // Sidebar toggle functionality
            $('#sidebarToggle').click(function() {
                $('#sidebar').toggleClass('collapsed');
                $('#main-content').toggleClass('expanded');
            });

            // Tab navigation
            $('[data-tab]').click(function(e) {
                e.preventDefault();
                const tabId = $(this).data('tab');
                $('.tab-pane').removeClass('show active');
                $('#' + tabId).addClass('show active');
                
                // Update active state in sidebar
                $('.sidebar-link').removeClass('active');
                $(this).addClass('active');
            });
        });
    </script>
</body>
</html>
